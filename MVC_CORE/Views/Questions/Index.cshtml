@model IEnumerable<MVC_CORE.Models.Questions>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/teacher.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead style="background-color: #cac8ed">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Questions_text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Questions_ans1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Questions_ans2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Questions_ans3)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Questions_anstrue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Questions_text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Questions_ans1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Questions_ans2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Questions_ans3)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Questions_anstrue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
